# =====================================================================
# 测试统一放宽项（仅测试 classpath，随 src/test/resources 加载，不打包进运行时 jar，
# 不影响生产 application.yml）。
#
# 写接口限流默认 10 次/用户/刷新周期（application.yml RATE_LIMIT_FOR_PERIOD）。
# 集成测试为单共享上下文 + 单用户高密度写：权限矩阵（M2 Appendix B 数十行）、
# 成员上限（200006）、规则上限（400004）、M2 审计 16 动作等用例，单一用户在一个
# 刷新周期内的写请求数远超 10，会被 RateLimitFilter 误判 100007。
# 限流逻辑正确性由 RateLimitFilterTest / RateLimitServiceTest 直接构造
# RateLimitService 并断言（不经 Spring 绑定），故此处放大额度不降低限流覆盖。
# =====================================================================
pocket-money.resilience.rate-limit.limit-for-period=100000
