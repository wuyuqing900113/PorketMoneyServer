# 本地开发环境：连接 config/docker/docker-compose.yml 启动的 PostgreSQL 18
spring:
  datasource:
    hikari:
      # 仅本地/测试开启连接泄漏检测
      leak-detection-threshold: 30000

logging:
  level:
    wyq.pocket.money: debug
