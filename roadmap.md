# 零花钱管理系统后端服务开发路线图（Roadmap）

> 依据：`mission.md`（使命与开发约束）、`tech-stack.md`（技术栈规范）
> 生成日期：2026-08-17
> 状态：草案 v1.0，供团队评审后基线化

---

## 1. 背景与现状

### 1.1 项目定位
为鸿蒙 APP 提供零花钱管理后端服务：家庭零花钱智能管理 + AI 语音交互 + 家长监管，采用**单体分层架构**，最终打包 Docker 镜像部署至阿里云。

### 1.2 当前状态
| 项目 | 状态 |
|---|---|
| Maven 工程骨架 | ✅ 已创建（`wyq.pocket.money` 包命名空间已就位） |
| Spring Boot 4.1.0 集成 | ❌ 未引入（pom.xml 尚无 parent 与依赖） |
| 业务代码 | ❌ 未开始（仅占位 `Main.java`） |
| 数据库 / CI / Docker | ❌ 未开始 |

### 1.3 关键量化约束（贯穿全程的验收基线）
| 维度 | 指标 |
|---|---|
| 性能 | API P95 ≤ 500ms；并发 ≥ 10 TPS |
| 可用性 | ≥ 99.5% |
| 质量 | 单元测试覆盖率 ≥ 80%；圈复杂度 ≤ 5；SonarQube 质量门禁通过 |
| AI 可信度 | AI 响应准确率 ≥ 95%；语音指令二次确认；决策可追溯 |
| 安全 | TLS 1.3；敏感数据 AES-256；OWASP 合规；儿童隐私（COPPA 类）合规 |
| 数据 | 每日增量 + 每周全量备份；关键操作审计日志 |

---

## 2. 里程碑总览

按 1~2 人小团队估算，总周期约 **19 周**（2026-08 下旬 ~ 2027-01 初）。人力变化时按比例调整，阶段顺序与验收标准不变。

| 里程碑 | 阶段 | 计划时间 | 核心产出 |
|---|---|---|---|
| **M0** | 工程基建与骨架 | 第 1–2 周（08-17 ~ 08-30） | 可运行的 Spring Boot 服务 + CI + 质量门禁 |
| **M1** | 认证与家庭域 | 第 3–4 周（08-31 ~ 09-13） | 登录/登出、JWT、家庭信息管理 |
| **M2** | 零花钱核心业务 | 第 5–8 周（09-14 ~ 10-11） | 看板、规则、收支记录、学习/工作价值零花钱 |
| **M3** | 移动端适配与可靠性 | 第 9–10 周（10-12 ~ 10-25） | 离线同步、幂等重试、性能达标 |
| **M4** | AI 交互集成 | 第 11–14 周（10-26 ~ 11-22） | Spring AI 接入、意图理解、语音链路、可信度机制 |
| **M5** | 通知与事件驱动 | 第 15 周（11-23 ~ 11-29） | 通知模块、Spring Event 解耦 |
| **M6** | 测试加固与安全 | 第 16–17 周（11-30 ~ 12-13） | 全量测试、性能/容量测试、安全测试 |
| **M7** | 部署与发布 | 第 18–19 周（12-14 ~ 12-27） | 阿里云生产部署、灰度发布、运维文档 |
| **GA** | 1.0 正式发布 | 2027-01 初 | 生产可用 + 文档齐套 |

```
M0 基建 → M1 认证 → M2 核心业务 → M3 可靠性 → M4 AI → M5 通知 → M6 加固 → M7 发布
  ████     ████      ████████       ████       ████████   ██      ████      ████
```

---

## 3. 阶段详细计划

### M0 工程基建与骨架（第 1–2 周）

**目标**：建立可持续交付的工程底座，让后续所有开发在统一规范下进行。

**关键任务**
1. `pom.xml` 引入 Spring Boot 4.1.0 parent、JDK 25 编译配置（启用虚拟线程）
2. 搭建分层包结构（`wyq.pocket.money` 下按 controller / service / mapper / domain 分层，业务模块独立包：user、money、rule、ai、finance、notify）
3. 集成核心依赖：MyBatis 3.5.x、HikariCP、Commons Lang3、Spring Boot Actuator、Logback（JSON 结构化日志）
4. PostgreSQL 18 本地开发环境（Docker Compose），建立数据库脚本管理（建表/变更脚本版本化，迁移工具选型待评审）
5. 统一响应体、统一错误码、全局异常处理、输入校验框架
6. 质量工具链：Checkstyle、PMD、SpotBugs 接入 Maven 构建；SonarQube 质量门禁规则配置
7. Git 工作流落地：分支规范（feature/bugfix/hotfix）、约定式提交、PR 评审流程
8. CI 流水线（Jenkins 或 GitHub Actions）：构建 → 单测 → 静态检查 → 制品归档

**验收标准（DoD）**
- [ ] `mvn clean verify` 一键通过，空壳服务可启动并暴露健康检查端点
- [ ] CI 流水线跑通，违反 Checkstyle/PMD 规则的代码被拦截
- [ ] 分层包结构与命名规范文档化
- [ ] 敏感配置零硬编码（环境变量/配置中心方案确定）

---

### M1 认证授权与家庭域（第 3–4 周）

**目标**：完成用户与家庭两个基础域，为所有业务功能提供身份与权限基础。

**关键任务**
1. Spring Security + JWT 认证体系（登录、登出、Token 刷新与失效）
2. OAuth2 支持（预留第三方登录扩展点）
3. 用户模块：注册/用户信息管理、角色模型（家长 / 孩子）、访问控制
4. 家庭信息管理：家庭的创建、查看、编辑；家庭成员关系维护
5. 密码与敏感字段 AES-256 加密存储；传输强制 TLS 1.3
6. 审计日志基础设施：登录、权限变更等关键操作落库
7. 安全日志：认证失败、越权尝试记录与告警


**验收标准（DoD）**
- [ ] 登录/登出 API 通过 RestAssured 集成测试，含异常场景（错误凭证、过期 Token、越权访问）
- [ ] 未认证请求一律拒绝；孩子角色无法访问家长专属接口（权限矩阵用例覆盖）
- [ ] 用户/家庭模块单测覆盖率 ≥ 80%
- [ ] API 文档（认证部分）产出

---

### M2 零花钱核心业务（第 5–8 周）

**目标**：交付 APP 首页与"我的"页面的全部非 AI 业务能力。

**关键任务**
1. **规则模块**：包月规则 CRUD、规则列表、个性化规则配置；规则引擎计算（余额按规则自动结算）
2. **零花钱模块**：
   - 家庭成员零花钱看板（余额汇总、趋势数据）
   - 零花钱增加（手动录入）
   - 提取记录（支出流水、历史查询）
   - 全部金额字段 DECIMAL 精度处理，事务保证一致性
3. **学习未来价值零花钱**：学习任务定义 → 完成确认 → 奖励发放链路
4. **工作价值零花钱**：家长工作价值记录与配套零花钱分配
5. **财务模块**：收支报表、家庭维度统计
6. 数据库设计评审：第三范式、索引设计（覆盖看板与记录查询）、避免 N+1
7. 耗时操作（报表统计等）异步化

**验收标准（DoD）**
- [ ] 全部核心 API 有 MyBatis 参数化查询（`#{}`），无 SQL 拼接
- [ ] 金额计算精度用例（浮点边界）全部通过；账务变动全事务化
- [ ] 看板/记录类查询 P95 ≤ 500ms（本地基准数据量下）
- [ ] 关键路径集成测试覆盖（Testcontainers + PostgreSQL）
- [ ] 单测覆盖率 ≥ 80%；SonarQube 门禁通过

---

### M3 移动端适配与可靠性（第 9–10 周）

**目标**：针对移动弱网环境与 mission 的可靠性约束做专项建设。

**关键任务**
1. 离线操作与数据同步方案：客户端操作幂等键设计、冲突处理策略
2. API 幂等性保障（防重复提交，尤其是资金变动类接口）
3. 重试与降级机制：外部依赖故障时的降级响应
4. 性能专项：索引调优、慢查询治理、慢接口剖析
5. JVM 调优基线：GC 选型（ZGC/Shenandoah）评估、堆内存参数、虚拟线程使用规范
6. 错误处理完善：统一错误码对客户端友好，支持客户端按错误码重试

**验收标准（DoD）**
- [ ] 资金类接口重复提交不会产生重复账务（幂等测试用例通过）
- [ ] 模拟弱网/超时场景下客户端可安全重试且数据一致
- [ ] 全量 API P95 ≤ 500ms（10 TPS 压测条件下）
- [ ] 同步协议 API 文档完成

---

### M4 AI 交互集成（第 11–14 周）

**目标**：落地 AI 语音交互能力，并同步实现 mission 中的五项可信度约束。

**关键任务**
1. **Spring AI 2.0.0 接入**：统一模型抽象层，接入大模型提供商（提供商选型为前置决策项），统一 JSON 响应格式
2. **意图理解（NLU）**：语音指令 → 意图解析 → 零花钱操作映射（查询余额、记账、增加/提取等）
3. **AI Function Calling**：AI 调用业务接口执行实际操作，操作走既有事务与权限体系
4. **ASR / TTS 集成**：语音识别与合成服务接入（供应商选型为前置决策项）
5. **可信度机制（与 mission 约束一一对应）**：
   - 准确性：AI 回答余额等数据必须实时查询真实账户，禁止模型自由生成；建立 AI 响应准确率评测集（目标 ≥ 95%）
   - 安全性：AI 会话强制身份验证；AI 可执行操作受角色权限约束；资金类语音指令二次确认流程
   - 一致性：AI 规则解释与规则引擎实际执行同源（解释文案由规则引擎输出，不由模型即兴生成）
   - 可解释性：AI 操作执行路径日志（意图 → 参数 → 调用链 → 结果）全程落审计
   - 隐私：语音数据不落盘持久化、传输加密；会话记录定期清理任务
6. **稳定性**：AI 调用超时控制（默认 30s）、限流（防单用户滥用）、异常降级（AI 不可用时回退手动操作入口）
7. Embedding 库接入：语义搜索/匹配能力（如规则与指令的语义匹配）

**验收标准（DoD）**
- [ ] 语音指令"查询余额 → 二次确认 → 执行 → 结果反馈"全链路演示通过
- [ ] AI 评测集准确率 ≥ 95%，评测报告存档
- [ ] AI 服务不可用时系统核心功能不受影响（降级演练通过）
- [ ] AI 操作审计日志可完整追溯任一会话的执行路径
- [ ] 语音数据持久化检查为"零残留"（合规自查通过）

---

### M5 通知与事件驱动（第 15 周）

**目标**：补齐通知模块，并用事件机制优化模块间耦合。

**关键任务**
1. 通知模块：账务变动通知、规则到期/余额不足提醒、AI 操作结果通知（推送通道选型待定）
2. Spring Event 事件驱动改造：账务变动 → 通知/看板刷新/审计等下游以事件解耦
3. 通知发送失败重试与记录

**验收标准（DoD）**
- [ ] 关键业务事件触发通知的集成测试通过
- [ ] 模块间无跨层直接调用（依赖关系审查通过）

---

### M6 测试加固与安全专项（第 16–17 周）

**目标**：发布前完成 mission 测试约束的全部项目。

**关键任务**
1. 单测补全：全模块覆盖率 ≥ 80%（JaCoCo 报告）
2. 集成测试：关键路径全覆盖（Spring Boot Test + Testcontainers + RestAssured）；外部 AI/语音依赖用 WireMock 模拟
3. **性能测试**（发布前置条件）：JMeter 按 10 TPS 容量场景压测，验证 P95 ≤ 500ms、无资源泄漏
4. **安全测试**：OWASP Top 10 自查（SQL 注入、XSS、CSRF、越权）；认证与加密实现审查；建立季度渗透测试机制（首轮安排在 GA 前）
5. 数据合规自查：儿童个人信息保护条款核对、日志脱敏规则验证、测试数据脱敏流程验证

**验收标准（DoD）**
- [ ] 性能测试报告：10 TPS 下 P95 ≤ 500ms、错误率达标
- [ ] 安全测试报告无高危项；中低危项有处置计划
- [ ] 覆盖率与 SonarQube 报告达到质量门禁

---

### M7 部署与发布（第 18–19 周）

**目标**：完成阿里云生产环境部署与发布机制，达成 GA。

**关键任务**
1. Docker 镜像构建（多阶段构建、JVM 参数与 GC 配置、健康检查）
2. 阿里云资源就绪：ECS、RDS（PostgreSQL）、SLB、OSS、容器镜像服务（镜像托管）
3. 环境隔离：开发 / 测试 / 生产三套环境与配置管理（环境变量或配置中心）
4. CI/CD 完善：构建 → 测试 → 镜像推送 → 部署；灰度发布与一键回滚机制；蓝绿部署验证零停机
5. 监控告警：Actuator 指标接入、阿里云 ARMS 应用监控、SLS 结构化日志接入（含敏感信息脱敏验证）
6. 备份与容灾：RDS 每日增量 + 每周全量备份策略生效；恢复演练一次
7. 文档齐套：API 文档、部署文档、运维手册、开发指南、安全操作手册

**验收标准（DoD）**
- [ ] 生产环境健康检查通过，灰度发布 → 全量流程演练成功
- [ ] 备份恢复演练成功（可恢复到指定时间点）
- [ ] 可用性监控覆盖核心接口，告警链路演练通过
- [ ] 五类文档评审归档

---

### GA 后：持续运营（长期）

| 事项 | 节奏 |
|---|---|
| 依赖安全更新与版本升级 | 持续跟踪，按发布周期升级，严格测试后上线 |
| 渗透测试 | 每季度一次 |
| 性能评估与优化 | 定期（结合监控数据） |
| 技术债务评估与偿还 | 每迭代预留容量 |
| 灾难恢复演练 | 定期 |
| AI 准确率回归评测 | 模型/Prompt 每次变更后 |

---

## 4. 跨阶段约束（每个阶段都必须满足）

1. **代码规范**：所有产出代码遵循 `code-style-guide.md`；PR 至少一人评审；禁止空 catch、禁止魔法值、DO/DTO/VO 分层。
2. **安全红线**：零硬编码密钥；全输入校验；MyBatis 一律 `#{}` 参数化；日志脱敏。
3. **测试伴随**：功能与单测同提交，不允许"先欠测试后补"；任何功能不得跳过测试阶段。
4. **文档伴随**：API 变更同步更新文档，不积压到发布前。
5. **提交规范**：约定式提交 `<type>(<scope>): <subject>`，Issue 跟踪进度。

---

## 5. 关键风险与应对

| 风险 | 影响 | 应对 |
|---|---|---|
| Spring Boot 4.1.0 / JDK 25 较新，部分三方库兼容性未知 | 基建阶段受阻 | M0 优先做依赖兼容性验证 spike；备选：锁定已验证版本组合并记录 |
| 大模型 / ASR / TTS 供应商未选型 | M4 启动延迟 | 第 8 周前完成选型决策（含成本、合规、儿童隐私条款审查） |
| AI 准确率不达标（< 95%） | 可信度约束不满足 | 评测集前置（M4 第一周建立）；意图收敛到有限操作集；不达标则收窄 AI 可执行范围，仅保留查询类 |
| 资金数据一致性缺陷 | 严重信任事故 | 全事务 + 幂等 + 对账任务；账务类代码强制双人评审 |
| 儿童隐私合规（COPPA 类） | 法律风险 | 语音不落盘、数据最小化、家长同意机制在 M1 设计时纳入 |
| 单人/小团队进度风险 | 周期拉长 | 阶段顺序不变，允许时间平移；M2 内部可按模块再切小版本 |

---

## 6. 前置决策清单（需尽快拍板）

| # | 决策项 | 建议截止时间 | 影响阶段 |
|---|---|---|---|
| 1 | 数据库脚本迁移工具选型（Flyway / Liquibase / 手工脚本） | M0 内 | M0 |
| 2 | 大模型提供商与计费方案 | 第 8 周前 | M4 |
| 3 | ASR / TTS 服务商 | 第 8 周前 | M4 |
| 4 | 通知推送通道（鸿蒙 Push / 短信 / 站内信） | 第 12 周前 | M5 |
| 5 | 阿里云账号、资源配额与预算 | 第 16 周前 | M7 |
| 6 | CI 平台二选一（Jenkins / GitHub Actions） | M0 内 | M0 |

---

## 7. 约束追溯表（mission 要求 → 落地阶段）

| mission.md 约束 | 落地阶段 |
|---|---|
| 家庭看板、规则、收支记录 | M2 |
| 学习/工作价值零花钱 | M2 |
| AI 语音交互 + 五项可信度约束 | M4 |
| 登录/登出、家庭信息管理、规则设置 | M1 |
| RESTful、P95 ≤ 500ms、弱网容错、离线同步 | M0（规范）/ M3（专项） |
| 单测 ≥ 80%、集成测试、性能测试、季度渗透测试 | 全程 / M6 |
| Docker + 阿里云、灰度回滚、环境隔离 | M7 |
| 备份策略、审计日志、数据脱敏 | M1（审计）/ M6（脱敏验证）/ M7（备份） |
| 监控与日志（Actuator / ARMS / SLS） | M0（本地）/ M7（云上接入） |
| 文档五件套 | 各阶段伴随，M7 齐套验收 |

---

## 8. 备注

- 本路线图基于当前 1~2 人团队规模估算；如人力变化，调整各阶段时长而非顺序与验收标准。
- 各阶段验收标准即该阶段的"完成定义"（DoD），未达 DoD 不进入下一阶段。
- 本文件与 `mission.md`、`tech-stack.md` 冲突时，以后两者为准并同步修订本文件。
